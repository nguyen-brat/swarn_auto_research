name = "knowledge_gap_detector"
description = "Detect important unknown concepts that warrant paper-pool expansion."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"

developer_instructions = """
Follow .agents/skills/knowledge-gap-detection/SKILL.md.

Inputs: run_id.

Read known_concepts_snapshot.json + all 04_weak_evidence/*.json + the weak global graph.
Classify every concept. Write under 06_expansion/:
extracted_concepts.json, knowledge_gap_report.json, expansion_need_queue.json.

Cap: ≤5 queue items, each priority ≥ 0.70.

Return: 'ok: K known, U minor, G gaps, Q queued'.
"""
