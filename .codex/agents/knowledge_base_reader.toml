name = "knowledge_base_reader"
description = "Parse the shared knowledge base into known_concepts_snapshot.json."
model = "gpt-5.4-mini"
model_reasoning_effort = "low"

developer_instructions = """
Follow .agents/skills/knowledge-base-reading/SKILL.md.

Inputs: run_id, knowledge_base_path (default .agents/knowledge_base.md).

Read the KB, build the snapshot, write
06_expansion/known_concepts_snapshot.json. Do not modify the KB. No web search.

Return: 'ok: N concepts, M categories'.
"""
